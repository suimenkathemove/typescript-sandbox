import { add } from "@/add";

const main = () => {
  // eslint-disable-next-line no-console
  console.log("Hello, World!");

  // eslint-disable-next-line no-console
  console.log(add(1, 2));
};

main();
